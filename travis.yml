language: cpp
compiler:
  - gcc
  - clang
  
branches:
  - master
  - testing

before_install:
  - sudo apt-add-repository ppa:beineri/opt-qt551 -y
  - sudo apt-get update -q
  - sudo apt-get install qt551-meta libgtest-dev

script:
  - source /opt/qt551/bin/qt551-env.sh
  - qmake
  - make